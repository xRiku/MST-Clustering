#include "../include/grouping.h"


/** Pseudo código da função de comparar pra eu não esquecer depois
 * IF id do set diferente
 *     retorna comparação de id do set
 * ELSE
 *     retorna comparação lexicográfica (q eu n sei qual é ainda)
**/